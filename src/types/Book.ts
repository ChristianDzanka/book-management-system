export interface Book {
  id: number;       // Unique identifier
  title: string;    // Book title
  author: string;   // Book author
  year: number;     // Publication year
  category: string; // e.g., Fiction, Science, Biography
}
